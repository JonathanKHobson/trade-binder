/**
 * Static public-contact configuration. Keep a real, intentionally public
 * address here once Kyle chooses the address that should receive trade mail.
 * Empty means the prototype offers the GitHub contact route instead of
 * misleading visitors with an undeliverable email action.
 */
export const tradeContact = {
  email: "",
  githubUrl: "https://github.com/JonathanKHobson",
} as const;
